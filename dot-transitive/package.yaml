name:                dot-transitive
version:             0.1.0.0
#synopsis:
#description:

dependencies:
  - base >= 4.7 && < 5
  - containers
  - doctest
  - dot-graph
  - graph-wrapper
  - language-dot

executables:
  dot-transitive:
    source-dirs:      src
    main:             Main.hs
    options:          -W -Wall
