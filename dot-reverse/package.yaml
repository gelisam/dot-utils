name:                dot-reverse
version:             0.1.0.0
#synopsis:
#description:

dependencies:
  - base >= 4.7 && < 5
  - containers
  - doctest
  - dot-graph
  - graph-wrapper
  - language-dot

ghc-options:      -W -Wall

library:
  source-dirs:      src

executables:
  dot-reverse:
    source-dirs:      app
    main:             Main.hs
    dependencies:     dot-reverse
